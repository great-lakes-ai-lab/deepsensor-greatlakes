# ConvNP Training Pipeline (Canonical Notebook)

This notebook trains a ConvNP model on GLSEA + ice concentration + static fields.
It is intended as the single source of truth for full-scale training runs.
